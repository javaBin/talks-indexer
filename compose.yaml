services:
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch-wolfi:9.2.3
    container_name: talks-indexer-elasticsearch
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ports:
      - "9200:9200"
      - "9300:9300"
    volumes:
      - elasticsearch-data:/usr/share/elasticsearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:9200/_cluster/health | grep -q '\"status\":\"green\"\\|\"status\":\"yellow\"'"]
      interval: 10s
      timeout: 5s
      retries: 10

  # talks-indexer:
  #   build: .
  #   container_name: talks-indexer-app
  #   environment:
  #     - MORESLEEP_URL=http://host.docker.internal:8082
  #     - MORESLEEP_USER=${MORESLEEP_USER:-}
  #     - MORESLEEP_PASSWORD=${MORESLEEP_PASSWORD:-}
  #     - ELASTICSEARCH_URL=http://elasticsearch:9200
  #     - PRIVATE_INDEX=javazone_private
  #     - PUBLIC_INDEX=javazone_public
  #     - PORT=8080
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     elasticsearch:
  #       condition: service_healthy
  #   extra_hosts:
  #     - "host.docker.internal:host-gateway"

volumes:
  elasticsearch-data:
